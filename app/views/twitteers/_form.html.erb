<%= form_with(model: twitteer, local: true) do |form| %>
  <% if twitteer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(twitteer.errors.count, "error") %> prohibited this twitteer from being saved:</h2>

      <ul>
        <% twitteer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :twitteer %>
    <%= form.text_area :twitteer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
